declare global {
	export namespace JSX {
		// TODO: the commented interface below should be used if we were to abide by strict typing. but we currently don't.
		// export interface IntrinsicElements extends HTMLElementTagNameMap { }
		// export type IntrinsicElements = { [tag_name: string]: any }
	}
}
